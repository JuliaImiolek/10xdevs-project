---
description: Guidelines for creating and maintaining GitHub Actions workflows
globs: .github/workflows/*.yml,.github/workflows/*.yaml
alwaysApply: false
---

# Github Action Rules

- Check if `package.json` exists in project root and summarize key scripts
- Check if `.nvmrc` exists in project root
- Check if `.env.example` exists in project root to identify key `env:` variables
- Always use `git branch -a | cat` to verify whether we use `main` or `master` branch
- Always use `env:` variables and secrets attached to jobs instead of global workflows
- Always use `npm ci` for Node-based dependency setup
- Extract common steps into composite actions in separate files

## Final verification steps

Once the workflow is done, run these checks:

1. **For each public action**, use Run Terminal to get the latest major version:

```bash
curl -s https://api.github.com/repos/{owner}/{repo}/releases/latest | grep '"tag_name":' | sed -E 's/.*"v([0-9]+).*/\1/'
```

2. **(Ask if needed)** Use Run Terminal to fetch README.md and confirm we're not using deprecated actions:

```bash
curl -s https://raw.githubusercontent.com/{owner}/{repo}/refs/tags/v{TAG_VERSION}/README.md
```

3. **(Ask if needed)** Use Run Terminal to fetch repo metadata and confirm the action is not archived:

```bash
curl -s https://api.github.com/repos/{owner}/{repo} | grep '"archived":'
```

4. **(Ask if needed)** For linter issues related to action parameters, fetch the action definition from GitHub:

```bash
curl -s https://raw.githubusercontent.com/{owner}/{repo}/refs/heads/{main/master}/action.yml
```
