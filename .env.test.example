# Use the same Supabase project where the seed user exists (see supabase/migrations/*_seed_default_user.sql).
SUPABASE_URL=###
# Anon (public) key from Dashboard → Settings → API → "anon public". Must be a JWT starting with eyJ (not sb_publishable_*).
SUPABASE_KEY=###

E2E_USERNAME=###
E2E_PASSWORD=###
